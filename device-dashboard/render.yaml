services:
  - type: web
    name: device-dashboard-backend
    env: node
    buildCommand: echo "No build needed"
    startCommand: node live_sse_server.js
    envVars:
      - key: NODE_ENV
        value: production
    healthCheckPath: /

